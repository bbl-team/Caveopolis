package com.benbenlaw.caveopolis.event.client;

import com.benbenlaw.caveopolis.Caveopolis;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.common.EventBusSubscriber;

//@EventBusSubscriber(modid = Caveopolis.MOD_ID, bus = EventBusSubscriber.Bus.MOD ,value = Dist.CLIENT)
public class ClientListener {

    /*
    @SubscribeEvent
    public static void registerOverlay(CustomizeGuiOverlayEvent event) {
        event.registerAbove(VanillaGuiOverlay.CROSSHAIR.id(), "lit_status", (gui, guiGraphics, partialTick, screenWidth, screenHeight)
                -> HoverInWorldEventsCaveopolis.renderLitStatusInfo(guiGraphics, partialTick));
    }

     */
}
